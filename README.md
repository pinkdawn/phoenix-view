# phoenix-view
Ambari View for Phoenix and HBase

# added from this repo

* add utf-8 support
* change it to normal java web, and add run tools

# How to run

* install maven \ node \ npm \ bower
* change to project directory, run `bower install`
* change 
* run `mvn jetty:run`, and visit `http:\\127.0.0.1:8080`
